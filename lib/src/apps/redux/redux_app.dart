import 'package:flutter/material.dart';

import './ui/redux_home.dart';

class ReduxApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(home: ReduxHome());
  }
}
