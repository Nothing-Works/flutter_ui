import 'package:flutter/material.dart';
import 'package:flutter_ui/src/apps/login/screens/home.dart';

class LoginApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Home(),
    );
  }
}
